pipeline {
   agent {dockerfile true}
    stages {
        stage('xxx') {
            steps {
            script {
            sh 'node --version'
            sh 'svn --version'
               }
            }
        }
    }
}
